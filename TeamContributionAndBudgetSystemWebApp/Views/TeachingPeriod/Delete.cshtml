@model TeamContributionAndBudgetSystemWebApp.Models.TCABS_Db_Context

@{
   ViewBag.Title = "Delete Teaching Period";
}

<title>Delete</title>
<h3>Are you sure you want to delete this?</h3>
<div>
   <h4>Teaching Period</h4>
   <hr />
   <dl class="dl-horizontal">
      <dt>
         @Html.DisplayNameFor( model => model.TeachingPeriod.Name )
      </dt>

      <dd>
         @Html.DisplayFor( model => model.TeachingPeriod.Name )
      </dd>

   </dl>

   @using( Html.BeginForm( ) )
   {
      @Html.AntiForgeryToken( )
      @Html.ValidationSummary( true, "", new { @class = "text-danger" } )
      @Html.HiddenFor( model => model.TeachingPeriod.TeachingPeriodId )

      <div class="form-group">
         <input type="submit" value="Delete" class="btn btn-default" /> |
         @Html.ActionLink( "Back to List", "Index" )
      </div>
   }
</div>
